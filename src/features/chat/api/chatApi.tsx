//import { baseApi } from "../../../shared/api/http/baseApi";

export const chatApi = {
  // getMessages: (chatId: string): Promise<Message[]> =>
  //   baseApi.get(`/chats/${chatId}/messages`),
  // sendMessage: (chatId: string, content: string): Promise<Message> =>
  //   baseApi.post(`/chats/${chatId}/messages`, { content }),
  // createChat: (participants: string[]): Promise<Chat> =>
  //   baseApi.post("/chats", { participants }),
};
